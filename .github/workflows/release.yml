name: Release

on:
  push:
    tags:
      - 'v*'

env:
  BUILD_TYPE: Release
  VCPKG_DEFAULT_BINARY_CACHE: ${{ github.workspace }}/vcpkg-cache
  VCPKG_BINARY_SOURCES: clear;files,${{ github.workspace }}/vcpkg-cache,readwrite

jobs:
  build-release:
    name: Build Release
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Validate version
        shell: pwsh
        run: |
          $tagVersion = "${{ github.ref_name }}" -replace '^v', ''
          $fileVersion = (Get-Content VERSION).Trim()
          if ($tagVersion -ne $fileVersion) {
            Write-Host "::error::Tag version ($tagVersion) does not match VERSION file ($fileVersion)"
            exit 1
          }
          Write-Host "Version validated: $fileVersion"

      - name: Create vcpkg cache directory
        run: mkdir -p "${{ env.VCPKG_DEFAULT_BINARY_CACHE }}"
        shell: bash

      - name: Restore vcpkg binary cache
        uses: actions/cache/restore@v4
        with:
          path: ${{ env.VCPKG_DEFAULT_BINARY_CACHE }}
          key: vcpkg-binary-${{ runner.os }}-${{ hashFiles('vcpkg.json') }}
          restore-keys: |
            vcpkg-binary-${{ runner.os }}-

      - name: Setup vcpkg
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgGitCommitId: '84bab45d415d22042bd0b9081aea57f362da3f35'

      - name: Setup MSVC environment
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure CMake
        run: |
          cmake -B build -S . -G Ninja `
            -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} `
            -DCMAKE_TOOLCHAIN_FILE="${{ github.workspace }}/vcpkg/scripts/buildsystems/vcpkg.cmake"

      - name: Save vcpkg binary cache
        uses: actions/cache/save@v4
        if: always()
        with:
          path: ${{ env.VCPKG_DEFAULT_BINARY_CACHE }}
          key: vcpkg-binary-${{ runner.os }}-${{ hashFiles('vcpkg.json') }}

      - name: Build
        run: cmake --build build --config ${{ env.BUILD_TYPE }}

      - name: Run tests
        run: ctest --test-dir ./build -C ${{ env.BUILD_TYPE }} --output-on-failure

      - name: Create release package
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}"
          $releaseDir = "SpotlightRenderer-$version"

          New-Item -ItemType Directory -Force -Path $releaseDir

          Copy-Item "build/SpotlightRenderer.exe" -Destination $releaseDir
          Copy-Item "build/*.dll" -Destination $releaseDir
          Copy-Item -Recurse "build/shaders" -Destination "$releaseDir/shaders"
          Copy-Item -Recurse "build/data" -Destination "$releaseDir/data"
          Copy-Item "README.md" -Destination $releaseDir -ErrorAction SilentlyContinue
          Copy-Item "LICENSE" -Destination $releaseDir -ErrorAction SilentlyContinue

          Compress-Archive -Path $releaseDir -DestinationPath "SpotlightRenderer-$version-win64.zip"

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-package
          path: SpotlightRenderer-*-win64.zip

  generate-docs:
    name: Generate Documentation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Doxygen
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen graphviz

      - name: Generate documentation
        run: doxygen Doxyfile

      - name: Upload documentation artifact
        uses: actions/upload-artifact@v4
        with:
          name: documentation
          path: docs/html/

  create-release:
    name: Create GitHub Release
    needs: [build-release, generate-docs]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download release package
        uses: actions/download-artifact@v4
        with:
          name: release-package
          path: ./artifacts

      - name: Download documentation
        uses: actions/download-artifact@v4
        with:
          name: documentation
          path: ./docs

      - name: Create documentation archive
        run: |
          cd docs
          zip -r ../SpotlightRenderer-${{ github.ref_name }}-docs.zip .

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Spotlight Renderer ${{ github.ref_name }}
          draft: false
          prerelease: ${{ contains(github.ref_name, '-alpha') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-rc') }}
          generate_release_notes: true
          files: |
            artifacts/*.zip
            SpotlightRenderer-${{ github.ref_name }}-docs.zip
